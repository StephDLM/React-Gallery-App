import React from 'react';
import Photos from './Photos';

const GifList = props => { 
  
  return(
    <ul className="photos-list">
      {/* <Gif /> */}
    </ul> 
  );
}

// export default PhotosList;